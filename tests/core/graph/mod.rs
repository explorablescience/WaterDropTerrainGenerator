//! Tests for `src/core/graph/`: `node_graph` covers evaluation/caching (`node_graph.rs` +
//! `state.rs`), `topology` covers pure graph wiring (`topology.rs`).

mod node_graph;
mod topology;
mod terrain_graph;
